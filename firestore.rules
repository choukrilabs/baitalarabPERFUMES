rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{document} {
      allow read: if true;
      allow write: if true; // Insecure for production, but suitable for this preview admin panel prototype
    }
  }
}
